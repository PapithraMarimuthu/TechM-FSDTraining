// 10. Utility Method in Package
package utility;

public class UtilityClass {
    // Method to return the length of an integer by converting it to a string
    public static int getLength(int num) {
        return String.valueOf(num).length();
    }
}
